# OSC 控制器简化测试与综合评价指标

## 1. 测试目的

用一次自动测试同时评价 OSC 控制器的：

1. **动态跟随性能**：实际 TCP 能否快速、准确地跟随目标 TCP；
2. **长期末端稳定性**：到达目标后，TCP 是否能长期保持稳定；
3. **零空间稳定性**：TCP 基本不动时，7 个关节是否仍发生缓慢漂移或自旋。

---

## 2. 简化测试条件

仅执行一次完整测试：

1. 机械臂从远离关节限位和奇异位形的安全中间姿态开始。
2. 同时施加一组 6D TCP 目标变化：
   - 平移：`X +50 mm，Y -40 mm，Z +40 mm`
   - 旋转：`Roll +12°，Pitch -10°，Yaw +15°`
3. 目标轨迹速度设置为当前允许 TCP 最大速度的 **80%**，各自由度同步到达目标。
4. 到达目标后保持 **30 s**。
5. 按完全相反的轨迹返回初始位姿，再保持 **30 s**。
6. 全程同步记录：
   - 目标 TCP 位置与姿态；
   - 实际 TCP 位置与姿态；
   - 7 个关节角。

该测试一次覆盖：

- 6D 耦合运动；
- 正向与反向跟随；
- 加速、匀速、减速和停车；
- 短期及长期末端稳定性；
- 冗余关节慢速漂移或自旋。

---

## 3. 三个归一化子指标

三个子指标均限制在 `0～1`，数值越大越好。

### 3.1 动态跟随得分 \(S_{\mathrm{track}}\)

在正向和反向运动阶段计算：

- TCP 位置跟踪 RMS 误差：\(E_p\)
- TCP 姿态跟踪 RMS 误差：\(E_R\)

\[
S_{\mathrm{track}}
=
\frac{1}{2}
\operatorname{clip}
\left(
1-\frac{E_p}{10\ \mathrm{mm}},
0,
1
\right)
+
\frac{1}{2}
\operatorname{clip}
\left(
1-\frac{E_R}{5^\circ},
0,
1
\right)
\]

其中：

- \(E_p=0\) 时，位置部分得满分；
- \(E_p\ge 10\ \mathrm{mm}\) 时，位置部分记为 0；
- \(E_R=0\) 时，姿态部分得满分；
- \(E_R\ge 5^\circ\) 时，姿态部分记为 0。

动态跟随是最重要的控制性能，在最终综合指标中权重最高。

---

### 3.2 长期保持得分 \(S_{\mathrm{hold}}\)

对两个 10 s 保持阶段分别计算，并取其中较差的一次。

设：

- TCP 最大位置漂移：\(D_p\)
- TCP 最大姿态漂移：\(D_R\)

漂移均相对于该保持阶段开始时的实际 TCP 位姿计算。

\[
S_{\mathrm{hold}}
=
\frac{1}{2}
\operatorname{clip}
\left(
1-\frac{D_p}{3\ \mathrm{mm}},
0,
1
\right)
+
\frac{1}{2}
\operatorname{clip}
\left(
1-\frac{D_R}{1.5^\circ},
0,
1
\right)
\]

其中：

- \(D_p\ge 3\ \mathrm{mm}\) 时，位置稳定部分记为 0；
- \(D_R\ge 1.5^\circ\) 时，姿态稳定部分记为 0。

该指标用于识别：

- 到达后继续缓慢移动；
- 短时间稳定但随后逐渐失稳；
- TCP 长期漂移或姿态缓慢偏转。

---

### 3.3 零空间稳定得分 \(S_{\mathrm{null}}\)

对两个 10 s 保持阶段分别计算，并取其中较差的一次。

对每个关节计算保持阶段内的最大偏移：

\[
D_{q,j}
=
\max_t
\left|
q_j(t)-q_j(t_0)
\right|
\]

取 7 个关节中最大值：

\[
D_q=\max_j D_{q,j}
\]

零空间稳定得分定义为：

\[
S_{\mathrm{null}}
=
\operatorname{clip}
\left(
1-\frac{D_q}{2^\circ},
0,
1
\right)
\]

其中：

- 所有关节保持不动，\(S_{\mathrm{null}}=1\)；
- 任一关节最大偏移达到或超过 \(2^\circ\)，\(S_{\mathrm{null}}=0\)。

使用“保持阶段内最大偏移”，而不是只比较首尾关节角，可避免关节先正转、后反转而导致净漂移接近零的漏检。

---

## 4. 单一综合指标：OSC Score

最终综合得分采用**加权负四阶幂平均，并对结果平方**：

\[
\boxed{
\mathrm{OSC\ Score}
=
100
\left[
0.60S_{\mathrm{track}}^{-4}
+
0.25S_{\mathrm{hold}}^{-4}
+
0.15S_{\mathrm{null}}^{-4}
\right]^{-1/2}
}
\]

权重分别为：

| 子指标 | 权重 |
|---|---:|
| 动态跟随 \(S_{\mathrm{track}}\) | 0.60 |
| 长期保持 \(S_{\mathrm{hold}}\) | 0.25 |
| 零空间稳定 \(S_{\mathrm{null}}\) | 0.15 |

### 数学意义

该公式具有以下特点：

1. **动态跟随权重最高**，仍是参数优化的首要目标；
2. 使用负四阶幂后，任一较差子指标会被显著放大；
3. 优秀指标不能轻易补偿明显短板；
4. 三个指标都接近优秀时，综合得分才会进入高分区；
5. 不需要额外设置人工限分规则。

当三个子指标完全相同时：

\[
S_{\mathrm{track}}
=
S_{\mathrm{hold}}
=
S_{\mathrm{null}}
=
S
\]

则：

\[
\mathrm{OSC\ Score}=100S^2
\]

因此高分区被自然拉开：

| 三个子指标均为 | OSC Score |
|---:|---:|
| 0.90 | 81.0 |
| 0.95 | 90.25 |
| 0.97 | 94.09 |
| 0.99 | 98.01 |
| 1.00 | 100.0 |

---

## 5. 得分解释

| OSC Score | 评价 |
|---:|---|
| `≥ 95` | 优秀，三方面均接近理想 |
| `90～94.9` | 良好，可作为候选最优参数 |
| `80～89.9` | 基本可用，但至少有一方面仍需优化 |
| `70～79.9` | 控制效果一般，存在较明显短板 |
| `< 70` | 控制效果不足 

---

