# OSC 真机只读遥测校准记录

采集时间：2026-08-10。采集期间 OSC 无活动会话，机器人处于 Follower/HOLD；未发送 CPV 位置、CPV 速度、夹爪或模式切换命令。

## 采集结果

| 项目 | 实测值 |
| --- | --- |
| CPV 加速度 `acc` / `dcc` | 7 轴均为 `1.0 rad/s²` |
| CPV 轮廓速度 `cv` | `[0.090, 0.090, 0.089, 0.089, 0.090, 0.090, 0.090] rad/s` |
| 位置环 `pp` | 7 轴均为 `5.0` |
| 速度环 `kp` | `[0.8, 0.8, 0.7, 0.7, 0.8, 0.8, 0.8]` |
| 速度环 `ki` | `[60, 60, 40, 40, 40, 40, 40]` |
| 反馈读取 p50 / p95 | `0.336 / 0.448 ms` |
| 20 ms 轮询请求周期 p95 | `21.358 ms` |

## 已应用的安全校准

- 影子 CPV 执行器：`max_joint_speed_rad_s=0.089`、`max_joint_acceleration_rad_s2=1.0`。
- 影子反馈延迟/抖动：`11.127 ms / 0.112 ms`。
- 真机估计器的 `max_prediction_s`：`32.484 ms`。
- 以上配置和完整原始参数保存于 `config/teleop.json` 的 `hardware_readonly_calibration`。

## 结论与边界

该只读采集能辨识 CPV 配置和 CAN 反馈读取时延，不能辨识“新 CPV 位置目标到真实关节响应”的执行器延迟、超调和时间常数。后者需要另行授权一个受限、可回退的微小 CPV 位置响应实验；在未完成该实验前，`position_time_constant_s` 与 `dispatch_delay_s` 仍是保守模型假设。

使用校准后 CPV 速度/加速度运行影子 OSC 基准，动态分显著低于此前理想影子模型，而长期保持和零空间稳定性仍接近满分。这说明真机与影子差异的主要可观测来源是 CPV 轮廓能力，而非 CAN 反馈读取耗时。
